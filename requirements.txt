# Core dependencies
instagrapi>=2.0.0
rich>=13.0.0
python-dotenv>=1.0.0
keyring>=24.0.0
cryptography>=41.0.0
pydantic>=2.0.0

# GUI dependencies (optional - only needed for GUI version)
customtkinter>=5.0.0
pillow>=10.0.0
